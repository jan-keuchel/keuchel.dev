###################
### KEYBINDINGS ###
###################

# Syntax: bind = MODS, key, dispatcher, params
# See https://wiki.hyprland.org/Configuring/Keywords/

$mod = ALT
$scriptDir = ~/.local/bin

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# Open special applications
   bind= $mod, Return, exec, $terminal  # Terminal
   bind = $mod SHIFT, Return, exec, $menu -matching fuzzy # Application launcher
bind        = $mod, E, exec, $fileManager     # File manager
bind= $mod  ,    B,   exec,     $browser # Brave browser
bind= $mod, M, exec, $mail          # thunderbird mail 
bind = $mod, G, exec, $gimp  # Gimp
bind= $mod, D, exec, $darktable  # darktable
bind    = $mod, V, exec, $virt_manager  # VMs
bind= $mod, Z, exec, zotero #Zotero
bind = $mod SHIFT, M, exec, $display_settings #Monitor layout and settings
